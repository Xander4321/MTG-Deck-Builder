<%= form_for @deck do |f| %>
  <%= f.label :name, "Name: " %>
  <%= f.text_field :name %>

  <%= f.label :deckstyle_id, "Format: " %>
  <%= f.select(:deckstyle_id, options_from_collection_for_select(@deckstyle, 'id', 'name')) %>

  <%= f.submit "Submit", class: "button" %>
<% end %>
